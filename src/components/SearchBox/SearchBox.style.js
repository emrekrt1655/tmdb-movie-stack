import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles((theme) => ({
    wrapper:{
        marginBottom: '1rem',
    },
    input:{
        width:'25rem',
    }
    
      
    
  }));